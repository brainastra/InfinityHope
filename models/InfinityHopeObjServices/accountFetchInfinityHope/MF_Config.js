define([],function(){
	var mappings = {
		"input" : "input",
	};
	Object.freeze(mappings);
	
	var typings = {
		"input" : "string",
	}
	Object.freeze(typings);
	
	var primaryKeys = [
	];
	Object.freeze(primaryKeys);
	
	var config = {
		mappings : mappings,
		typings : typings,
		primaryKeys : primaryKeys,
		serviceName : "InfinityHopeObjServices",
		tableName : "accountFetchInfinityHope"
	};
	Object.freeze(config);
	
	return config;
})
